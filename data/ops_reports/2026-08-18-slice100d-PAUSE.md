# Dilim 4 — duraklatıldı (2026-08-18 ~22:23 TR)

Anthropic batch **iptal edilmedi**. Sonuçlar henüz yok; yarın retrieve yeterli.

## Durum

| | |
|---|---|
| Batch | `msgbatch_01Y5QR4vEa4ub7FjtfwHUNzR` |
| `pending_batches.json` | `applied=false`, 100 job |
| ID listesi | `data/ops_reports/2026-08-18-slice100d-ids.txt` (100 id, 37 medium + 63 low) |
| Anthropic | `in_progress` — 0/100 succeeded, 0 error (~102 dk’de hâlâ 0) |
| DB | Dilim 4 verdict yok |
| Batch son kullanma | ~2026-08-19 20:40 TR (24 saat) |

## Yarın (kod değiştirme, --skip-nli yok)

```bash
cd "health_misinfo_monitor (1)"
./venv/bin/python pipeline/03_factcheck.py --batch-retrieve --wait --wait-timeout 7200 --poll-interval 20

IDS=$(cat data/ops_reports/2026-08-18-slice100d-ids.txt)
./venv/bin/python pipeline/12_ops_report.py --scope slice100d --claim-ids "$IDS" \
  --compare-to data/ops_reports/2026-08-18-slice100c.md --stdout
```

Batch `ended` değilse retrieve bekler; timeout olursa aynı komutu tekrar çalıştır.

## Bitmiş dilimler (diskte)

- Dilim 1: `2026-08-18-slice100.md` + `-ids.txt`
- Dilim 2: `2026-08-18-slice100b.md` + `-ids.txt`
- Dilim 3: `2026-08-18-slice100c.md` + `-ids.txt` (100 id; 97 batch + 3 NLI-only)

## Token tahmini (fatura değil) — Dilim 1–3

- 300 iddia, usage n=297, **toplam ~$10.39**
- ortalama **$0.0350 / claim**
- kalan ~254 (100 Dilim 4 uçuşta + ~154) × $0.0350 → 554 bittiğinde ~**$19.27**

Formül: batch %50 + cache write $2.50/M (batch %50) + cache read $0.20/M; Sonnet 5 $2/M in + $10/M out.
