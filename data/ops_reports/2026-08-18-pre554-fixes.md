# Pre-554 düzeltme + shadow paket — kanıt raporu

**Tarih:** 2026-08-18  
**554 kuyruğu bu turda çalıştırılmadı.** Teslim: kod, testler, all_verdicted baseline, dilim runbook.

Hiçbir madde mevcut auto-accept kurallarını gevşetmedi. Madde 2 yalnızca `compound_tier_mismatch`'i mevcut güvenlik zincirine ekledi (sıkılaştırma).

---

## Madde 1 — ops raporu takma ad hatası

**Düzeltme:** [`pipeline/12_ops_report.py:301`](../../pipeline/12_ops_report.py)

```
clauses.append("vr.claim_id IS NOT NULL")
```

JOIN zaten `vr`; eski `v.claim_id` `--scope all_verdicted`'i `no such column: v.claim_id` ile düşürüyordu.

**Komut:**

```bash
./venv/bin/python pipeline/12_ops_report.py --scope all_verdicted --stdout
```

**Çıktı (satır 8):**

```
- Verdict almış: **244**
```

**Test:**

```
tests/test_ops_report.py::test_all_verdicted_returns_244_rows PASSED
tests/test_ops_report.py::test_all_verdicted_sql_uses_vr_alias PASSED
```

`--out` / `--compare-to` eklendi; varsayılan `2026-08-18.md` (test kohortu) üzerine yazılmadı.

---

## Madde 2 — compound_tier_mismatch güvenlik zinciri

**Düzeltme:** [`utils/factcheck_review.py:47`](../../utils/factcheck_review.py) (`security_risk_triggers`) ve [`utils/factcheck_review.py:123`](../../utils/factcheck_review.py) (`compute_needs_human`).

```
if COMPOUND_TIER_MISMATCH_FLAG in flags:
    triggers.append("compound_tier_mismatch")
...
or (COMPOUND_TIER_MISMATCH_FLAG in flags)
```

`_reconcile_stale_auto_accepted()` aynı trigger listesini kullandığı için eski `auto_accepted=1` satırlar da düzelir.

**Test (a)** farklı-tier + `package_only_forced` yok → `auto_accepted=0`  
[`tests/test_factcheck_rules.py:181`](../../tests/test_factcheck_rules.py)

```
tests/test_factcheck_rules.py::test_compound_tier_mismatch_blocks_auto_accept_without_package_only PASSED
```

**Test (b)** aynı-tier → auto-accept adayı değişmez  
[`tests/test_factcheck_rules.py:221`](../../tests/test_factcheck_rules.py)

```
tests/test_factcheck_rules.py::test_compound_same_tier_auto_accept_unchanged PASSED
```

**Canlı DB:** aktif verdict'lerde `compound_tier_mismatch` flag = **1**; `auto_accepted=1 AND flag` = **0** (stale yok).

---

## Madde 3 — all_verdicted baseline

**Komut:**

```bash
./venv/bin/python pipeline/12_ops_report.py --scope all_verdicted \
  --out data/ops_reports/2026-08-18-all_verdicted-baseline.md
```

**Çıktı:**

```
[ops_report] yazıldı: data/ops_reports/2026-08-18-all_verdicted-baseline.md (n_claims=244, n_verdicts=244)
```

Dosya: [`data/ops_reports/2026-08-18-all_verdicted-baseline.md`](2026-08-18-all_verdicted-baseline.md)

554 sonrası alarm:

```bash
./venv/bin/python pipeline/12_ops_report.py --scope all_verdicted \
  --compare-to data/ops_reports/2026-08-18-all_verdicted-baseline.md \
  --out data/ops_reports/YYYY-MM-DD-all_verdicted.md
```

Aynı gün `YYYY-MM-DD.md` karşılaştırması bu baseline'ı kaçırır; `--compare-to` şart.

---

## Madde 4 — retrieve_hybrid_evidence try/except

**Düzeltme:** [`pipeline/03_factcheck.py:755-772`](../../pipeline/03_factcheck.py) — retrieval ayrı try/except; hata olursa claim atlanır, debug'a `retrieval_failed: true` yazılır, tur sonunda id listesi basılır ([`03_factcheck.py:987`](../../pipeline/03_factcheck.py)). Sessiz yutma yok.

**Test:** `retrieve_hybrid_evidence` 3. çağrıda `ConnectionError`; `--batch-submit --dump-payload`; kalan 2 id job'da, atlanan id raporda.

```
tests/test_factcheck_retrieval_skip.py::test_retrieve_failure_skips_third_claim_rest_enter_job PASSED
```

[`tests/test_factcheck_retrieval_skip.py`](../../tests/test_factcheck_retrieval_skip.py) — `assert skipped not in custom_ids`, `assert "1 iddia retrieval hatasıyla atlandı" in out`.

---

## Madde 5 — auto-accept shadow gate (üretim değişmedi)

`compute_needs_human()` madde 2 dışında dokunulmadı. Verdict/confidence eşiği üretime bağlanmadı.

Fonksiyon: [`utils/reviewer_summary.py:471`](../../utils/reviewer_summary.py) `compute_shadow_human_gates`.  
Kolonlar: [`db/schema.sql:82-85`](../../db/schema.sql) + `_migrate_columns` + `_write_verdict`.  
Backfill: [`utils/db.py:_backfill_shadow_human_gates`](../../utils/db.py) — mevcut satırlar salt okuma alanlarından dolduruldu.

**Aktif 244 verdict (tahmin değil, DB):**

| Sinyal | Sayı |
|--------|-----:|
| would_require_human_verdict_gate | **135** |
| would_require_human_confidence_gate | **187** |
| would_require_human_compound_gate | **0** (madde 2 sonrası regresyon kontrolü) |
| would_auto_accept_after_all_gates | **15** |
| auto_accepted (üretim, değişmedi-mantık) | 45 |
| human_reviewed | 11 (tüm DB; üretim yazımı aynı) |

554'e ölçekleme yok. Kesin 554-turu sayısı dilimler bitince aynı kolonlardan gelecek.

Shadow birim testleri: `tests/test_reviewer_summary.py::test_shadow_*` PASSED.

---

## Madde 6 — cost faz0/1 artıkları

### 6a — cache token formülü

[`pipeline/18_cost_faz1_test.py:28-42`](../../pipeline/18_cost_faz1_test.py) — `cache_creation_input_tokens` ($2.50/M) + `cache_read_input_tokens` ($0.20/M). Sync; batch %50 yok.

```
tests/test_cost_faz1.py::test_estimate_sync_cost_includes_cache_tokens PASSED
```

**+18.6% / +308.6% = formül düzeltmesi (aynı after kaydı, cache yok sayılıyordu).** Faz 1 before→after değil.

| claim_id | eski after $ (cache yok) | düzeltilmiş after $ (cache var) | formül farkı |
|---|---:|---:|---:|
| 1243 | 0.0091 | 0.0371 | **+308.6%** |
| 1284 | 0.0195 | 0.0231 | **+18.6%** |

**Faz 1 etkisi (aynı düzeltilmiş sync formül; jP5 log before vs debug after):**

| claim_id | jP5 before $ | Faz 1 after $ | Δ | % |
|---|---:|---:|---:|---:|
| 1243 | 0.2432 | 0.0371 | −0.2061 | **−84.7%** |
| 1284 | 0.0623 | 0.0231 | −0.0392 | **−62.9%** |
| 1247 | 0.1598 | 0.0572 | −0.1026 | −64.2% |
| 1248 | 0.0612 | 0.0401 | −0.0210 | −34.4% |

jP5 log cache basmıyor (cache=0 varsayımı → before **alt sınır**). Dört eşleşmede maliyet düştü; net −70.1% (sum $0.5265 → $0.1576).

Kaynak: [`data/ops_reports/2026-08-18-cost-faz1-test.md`](2026-08-18-cost-faz1-test.md), `data/smoke_jP5XF06OLbo/factcheck_20.log`

### 6b — subprocess check + after>before

[`pipeline/18_cost_faz1_test.py:122`](../../pipeline/18_cost_faz1_test.py) `subprocess.run(..., check=True)`.  
[`18_cost_faz1_test.py:83-100`](../../pipeline/18_cost_faz1_test.py) `_assert_after_newer` (`logged_at`). `--skip-run`'da atlanır.  
[`pipeline/03_factcheck.py:_append_debug_log`](../../pipeline/03_factcheck.py) ISO `logged_at` ekler.

### 6c — NLI REFUTES → 2 nerede yazılı (kod değişmedi)

| Kural | Alan | Yer |
|-------|------|-----|
| `NLI REFUTES → 2` | **`nli_label`** (HF ucuz filtre, escalate **öncesi**) | [`utils/claude_client.py:60-70`](../../utils/claude_client.py) `resolve_max_search_calls`; uygulanış [`pipeline/03_factcheck.py:838-841`](../../pipeline/03_factcheck.py) |
| `evidence_stance=contradicts` | LLM/NLI **çıktı** stance | [`db/schema.sql:75`](../../db/schema.sql); NLI-only map [`03_factcheck.py:912-914`](../../pipeline/03_factcheck.py) |

Aynı alan değil. `max_search_calls` `evidence_stance` okumaz. `initial_risk=high` → 3, REFUTES'u ezer.

```
tests/test_claude_escalate.py::test_resolve_max_search_calls_defaults PASSED
  REFUTES → 2, high → 3, SUPPORTS → 1
```

### 6d — $/claim varyans (ortalama değil)

p50/p90/p95/max **cache dahil** — `12_ops_report._estimate_cost_usd` (batch fiyat + cache write $2.50/M × 0.5 + cache read $0.20/M). Madde 6a öncesi `18_cost` eski formülü değil; ops raporu zaten cache sayıyordu.

all_verdicted n=87 (hepsi cache token'lı; 87/87 `cost_sources=batch`):

| | cache dahil (rapordaki sayı) | cache yok (karşılaştırma) |
|--|--:|--:|
| ortalama | **0.0393** | 0.0102 |
| **p50** | **0.0376** | 0.0090 |
| **p90** | **0.0561** | 0.0156 |
| **p95** | **0.0643** | 0.0178 |
| **max** | **0.0903** | 0.0292 |

Aynı 87 snapshot'ta cache eklemek tahmini **87/87 artırır** (ortalama +$0.0291) — bu Faz 1 before/after değil, eksik token alanının düzeltilmesi.

87'nin Faz 1 jP5 çifti **yalnızca 4 iddia** (1243, 1247, 1248, 1284): düzeltilmiş sync ile **4/4 düştü**, 0 arttı. Kalan 83'ün jP5 before usage'ı yok; 87 üzerinden "kaçında Faz 1 artırdı" sayılamaz.

---

## Madde 7 — embedding sessiz degradasyon

[`utils/text_similarity.py:97-106`](../../utils/text_similarity.py): dönüş `(clusters, status)` — `"ok"` veya `"failed: <sebep>"`.

[`pipeline/06_claim_index.py`](../../pipeline/06_claim_index.py): failed ise lexical fallback etiketlenir; sidecar `data/ops_reports/embedding_clustering_status.txt`.

Bu turda 06 koşulmadı. Ops rapor satırı: `ok (probe: sentence-transformers import; sidecar yok — 06_claim_index çalıştırın)`.

**Test:**

```
tests/test_text_similarity.py::test_embedding_clusters_failed_status PASSED
tests/test_text_similarity.py::test_embedding_clusters_cross_channel_perine PASSED
```

Perine testi `skipif(not _embedding_model_available())` — model cache'de olduğu için bu makinede koştu; ağ/model yoksa skip (yanlış kırmızı yok).

---

## Dilim sağlık alanları (12_ops_report)

all_verdicted baseline'daki gerçek sayılar:

```
processed (verdict almış)          244
parse_failed                       0
retrieval_failed                   0
web_search_call_count p50/p95/max  2.0 / 2.8 / 3.0 (n=5)
$/claim p50/p90/p95/max            $0.0376 / $0.0561 / $0.0643 / $0.0903
needs_human oranı                  81.6%
would_auto_accept_after_all_gates  15
compound_tier_mismatch sayısı      1
```

`web_search_call_count` n=5: debug jsonl'de alanın dolduğu kayıt sayısı (cite_source vekili değil). 554 dilimlerinde escalate sonuçlarıyla n artacak.

---

## 100'lük dilim runbook (554 henüz başlatılmadı)

`--limit` varsayılan 200; her dilimde `--limit 100`. ~6 dilim. Batch chunking kodu yok.

```bash
# N. dilim — submit
./venv/bin/python pipeline/03_factcheck.py --batch-submit --limit 100 --skip-nli

# pending applied=false (mevcut kontrole EK)
./venv/bin/python - <<'PY'
import json
from pathlib import Path
data = json.loads(Path("data/pending_batches.json").read_text())
pending = [b for b in data.get("batches") or [] if not b.get("applied")]
print(f"applied=false batch: {len(pending)}")
for b in pending:
    ids = b.get("claim_ids") or []
    print(f"  {b.get('batch_id')} n={len(ids)}")
    Path("/tmp/slice_ids.txt").write_text(",".join(str(i) for i in ids))
PY

# retrieve
./venv/bin/python pipeline/03_factcheck.py --batch-retrieve --wait

# dilim ops (yukarıdaki metriklere ek olarak)
./venv/bin/python pipeline/12_ops_report.py \
  --claim-ids "$(cat /tmp/slice_ids.txt)" --stdout
```

Şu an `pending_batches.json`: 3 batch, hepsi `applied: true`.

554 sonrası tüm kohort:

```bash
./venv/bin/python pipeline/12_ops_report.py --scope all_verdicted \
  --compare-to data/ops_reports/2026-08-18-all_verdicted-baseline.md --stdout
```

---

## Test özeti (bu paket)

```
./venv/bin/python -m pytest tests/test_factcheck_rules.py tests/test_reviewer_summary.py \
  tests/test_ops_report.py tests/test_text_similarity.py tests/test_cost_faz1.py \
  tests/test_factcheck_retrieval_skip.py tests/test_claude_escalate.py -q
```

**63 passed.**
